# Kubernetes (v3)

The v2 EKS manifests, load-test Jobs, and related YAML live under **`v2_obsolete/archive/deploy/kubernetes/`**. Recreate this directory when implementing v3 cluster deploy per **`plans/V3_ASYNC_PIPELINE_IMPLEMENTATION_PLAN.md`**.

Until then, workspace rules about in-cluster load tests apply **once** v3 manifests and a driver script exist again at the repository root.
