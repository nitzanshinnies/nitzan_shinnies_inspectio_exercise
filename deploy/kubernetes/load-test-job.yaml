# In-cluster performance driver (§28.6). Apply after workloads are healthy.
# Logs: kubectl -n inspectio logs job/inspectio-load-test -f
#
# Tweak args / env below; image must include scripts/ (see deploy/docker/Dockerfile).
apiVersion: batch/v1
kind: Job
metadata:
  name: inspectio-load-test
  namespace: inspectio
spec:
  backoffLimit: 0
  ttlSecondsAfterFinished: 86400
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: driver
          image: inspectio-app:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: INSPECTIO_LOAD_TEST_API_BASE
              value: http://inspectio-api:8000
          args:
            - python
            - scripts/full_flow_load_test.py
            - --kubernetes
            - --sizes
            - "1000"
            - --chunk-max
            - "1000"
            - --http-timeout-sec
            - "300"
            - --outcome-timeout-sec
            - "7200"
            - --outcome-limit
            - "20000"
            - --wait-outcomes
            - --json-summary
