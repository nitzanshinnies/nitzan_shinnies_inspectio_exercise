apiVersion: v1
kind: ServiceAccount
metadata:
  name: inspectio-app
  namespace: inspectio
  annotations:
    # Set this to your IAM role ARN in EKS.
    eks.amazonaws.com/role-arn: "REPLACE_WITH_IRSA_ROLE_ARN"
