/** Named limits aligned with plans/REST_API.md and API defaults (see `api/config.py`). */

export const DEFAULT_OUTCOME_LIMIT = 100;
export const OUTCOME_LIMIT_MAX = 1000;
export const OUTCOME_LIMIT_MIN = 1;

/** Mirrors `REPEAT_COUNT_MAX` on the API (plans/REST_API.md §3.2). */
export const REPEAT_COUNT_MAX = 10000;
export const REPEAT_COUNT_MIN = 1;
